%h1 Listing albums

%table#albums
  %thead
    %tr
      %th Title
      %th Artist
      %th Released at

  %tbody
    - @albums.each do |album|
      %tr      
        %td= link_to album.title, album
        %td= link_to album.artist.name, artist_albums_path(album.artist)
        %td= album.released_at

%br

= link_to 'New Album', new_album_path
