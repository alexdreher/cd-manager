%h1 Listing artists

%table
  %tr
    %th User
    %th Name
    %th Description
    %th
    %th
    %th

  - @artists.each do |artist|
    %tr
      %td= artist.user_id
      %td= artist.name
      %td= artist.description
      %td= link_to 'Show', artist
      %td= link_to 'Edit', edit_artist_path(artist)
      %td= link_to 'Destroy', artist, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Artist', new_artist_path
